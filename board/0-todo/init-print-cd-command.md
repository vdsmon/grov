# Print cd command after init

After `grov init`, the output should include a copy-pasteable `cd` command pointing to the newly created worktree directory (e.g., `cd project/prefix_main`). Make it easy for the user to jump right in.
